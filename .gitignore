*

!.gitignore
!.gitattributes
!flake.nix
!flake.lock

# modules
!modules/
!modules/**/*

# out-of-tree package sources referenced by modules (import-tree must not scan them)
!pkgs/
!pkgs/**/*

# sops: encrypted secrets + public-key policy are safe to publish
!secrets/
!secrets/**/*
!.sops.yaml
